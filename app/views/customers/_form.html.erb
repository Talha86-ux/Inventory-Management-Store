<%= form_with model: @customer, url: company_customers_path do |form| %>
  <% if customer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(customer.errors.count, "error") %> prohibited this customer from being saved:</h2>

      <ul>
        <% customer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, required:true %>
  </div>

  <div class="field">
    <%= form.label :cnic %>
    <%= form.text_field :cnic, required:true %>
  </div>
  <div class="field">

   <%c = Company.find_by(assign_manager: current_user.id) %>

    <%= form.number_field :company_id, value: c.id, type:"Hidden" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
